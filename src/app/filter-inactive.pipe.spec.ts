import { FilterInactivePipe } from './filter-inactive.pipe';

describe('FilterInactivePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInactivePipe();
    expect(pipe).toBeTruthy();
  });
});
