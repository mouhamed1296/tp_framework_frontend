import { CostumerrorDirective } from './costumerror.directive';

describe('CostumerrorDirective', () => {
  it('should create an instance', () => {
    const directive = new CostumerrorDirective();
    expect(directive).toBeTruthy();
  });
});
