import { SearchInactifPipe } from './search-inactif.pipe';

describe('SearchInactifPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInactifPipe();
    expect(pipe).toBeTruthy();
  });
});
